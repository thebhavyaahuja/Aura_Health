# Minimal requirements for Document Parsing Service
# Use this if the main requirements.txt has conflicts

# Core dependencies
fastapi
uvicorn[standard]
docling
sqlalchemy
httpx
pydantic
python-dotenv
